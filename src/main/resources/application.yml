spring:
  datasource:
    username: chenxi
    password: ChenXi_10
    url: jdbc:mysql://1.15.1.240:3306/star_bigdata?characterEcoding=utf-8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: star_bigdata
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  #  主键自增
  global-config:
    db-config:
      id-type: auto

server:
  port: 8000